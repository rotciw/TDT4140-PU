/**
 * This is the main Firebase integration file.
 * Reading from Firebase is done directly in the view files.
 * Writing and updating is done here, so we get consistent data structures.
 */

// import { firestore, environment } from './configuration'

const users = {
  createUser () {
    return null
  }
}

export default {
  users
}
